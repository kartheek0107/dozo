<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_orange"
    android:fillViewport="true">

    <LinearLayout
        android:id="@+id/mainLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp">

        <!-- Logo Card -->
        <LinearLayout
            android:layout_width="160dp"
            android:layout_height="160dp"
            android:orientation="vertical"
            android:gravity="center"
            android:background="@drawable/white_card_bg"
            android:elevation="6dp"
            android:layout_marginTop="60dp"
            android:layout_marginBottom="28dp">

            <ImageView
                android:id="@+id/imgLogo"
                android:layout_width="96dp"
                android:layout_height="96dp"
                android:src="@drawable/ic_shopping_cart"
                android:contentDescription="App Logo"
                app:tint="#FF5722" />
        </LinearLayout>

        <!-- App Title -->
        <TextView
            android:id="@+id/tvAppTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textColor="#FFFFFF"
            android:textSize="36sp"
            android:textStyle="bold"
            android:layout_marginBottom="8dp" />

        <!-- Tagline -->
        <TextView
            android:id="@+id/tvTagline"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Market made social 🛒"
            android:textColor="#FFEFEF"
            android:textSize="16sp"
            android:layout_marginBottom="40dp"
            android:gravity="center"
            android:textAlignment="center" />

        <!-- Info Card -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp"
            android:background="@drawable/white_rounded_card"
            android:elevation="8dp"
            android:layout_marginBottom="40dp">

            <TextView
                android:id="@+id/tvDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Small Basket connects those out in the market with students or locals staying back home or in hostels. Need a snack, a notebook, or something small? Ask, and someone nearby will grab it for you — earn or help in return!"
                android:textColor="#333333"
                android:textSize="15sp"
                android:textStyle="italic"
                android:lineSpacingExtra="4dp"
                android:gravity="center"
                android:textAlignment="center" />
        </LinearLayout>

        <!-- Buttons -->
        <Button
            android:id="@+id/btnGoToRegister"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Get Started"
            android:textAllCaps="false"
            android:backgroundTint="#FFFFFF"
            android:textColor="#FF5722"
            android:textStyle="bold"
            android:padding="14dp"
            android:elevation="6dp"
            android:layout_marginBottom="16dp"
            android:layout_marginHorizontal="20dp" />

        <TextView
            android:id="@+id/btnGoToLogin"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Already have an account? Login"
            android:textColor="#FFF"
            android:textSize="15sp"
            android:textStyle="bold"
            android:layout_marginBottom="80dp" />

    </LinearLayout>
</ScrollView>
