<?xml version="1.0" encoding="utf-8"?>
<!-- NOTIFICATION ITEM — fully aligned to app's teal design language.
     All IDs are unchanged. Only blue (#3B82F6) → teal (#14B8A6 / #0F766E)
     colour swaps and minor spacing polish applied. -->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="@android:color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="?attr/selectableItemBackground"
        android:paddingStart="4dp">

        <!-- Left unread indicator bar — teal to match app accent -->
        <View
            android:id="@+id/unreadIndicator"
            android:layout_width="4dp"
            android:layout_height="match_parent"
            android:layout_marginVertical="12dp"
            android:background="@drawable/bg_input_rounded"
            android:backgroundTint="#14B8A6"
            android:visibility="visible"
            tools:visibility="visible" />

        <!-- Content row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="16dp"
            android:gravity="center_vertical">

            <!-- Icon container — teal tinted, matching app's icon card style -->
            <androidx.cardview.widget.CardView
                android:id="@+id/iconContainer"
                android:layout_width="52dp"
                android:layout_height="52dp"
                app:cardCornerRadius="14dp"
                app:cardElevation="0dp"
                app:cardBackgroundColor="#E0F2F1">

                <ImageView
                    android:id="@+id/ivNotificationIcon"
                    android:layout_width="26dp"
                    android:layout_height="26dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_package"
                    app:tint="#14B8A6"
                    tools:src="@drawable/ic_package" />
            </androidx.cardview.widget.CardView>

            <!-- Text content -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="14dp"
                android:layout_marginEnd="8dp"
                android:orientation="vertical">

                <!-- Title row with unread dot -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:layout_marginBottom="4dp">

                    <!-- Unread dot — teal -->
                    <View
                        android:id="@+id/unreadDot"
                        android:layout_width="8dp"
                        android:layout_height="8dp"
                        android:layout_marginEnd="8dp"
                        android:background="@drawable/notification_badge_bg"
                        android:backgroundTint="#14B8A6"
                        android:visibility="gone"
                        tools:visibility="visible" />

                    <TextView
                        android:id="@+id/tvNotificationTitle"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:textColor="#111827"
                        android:textSize="15sp"
                        android:textStyle="bold"
                        android:fontFamily="sans-serif-medium"
                        android:maxLines="1"
                        android:ellipsize="end"
                        tools:text="New Delivery Request" />
                </LinearLayout>

                <!-- Body text -->
                <TextView
                    android:id="@+id/tvNotificationMessage"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textColor="#6B7280"
                    android:textSize="13sp"
                    android:lineSpacingMultiplier="1.3"
                    android:maxLines="2"
                    android:ellipsize="end"
                    android:layout_marginBottom="10dp"
                    tools:text="Someone needs lunch delivered from Canteen to Hostel A." />

                <!-- Metadata row: time + category badge -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">

                    <!-- Timestamp -->
                    <LinearLayout
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:orientation="horizontal"
                        android:gravity="center_vertical">

                        <ImageView
                            android:layout_width="12dp"
                            android:layout_height="12dp"
                            android:src="@android:drawable/ic_menu_recent_history"
                            app:tint="#9CA3AF"
                            android:contentDescription="Time" />

                        <TextView
                            android:id="@+id/tvNotificationTime"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginStart="5dp"
                            android:textColor="#9CA3AF"
                            android:textSize="12sp"
                            tools:text="5 mins ago" />
                    </LinearLayout>

                    <!-- Category badge — teal themed, matches app identity -->
                    <androidx.cardview.widget.CardView
                        android:id="@+id/categoryBadge"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        app:cardCornerRadius="10dp"
                        app:cardElevation="0dp"
                        app:cardBackgroundColor="#E0F2F1">

                        <TextView
                            android:id="@+id/tvNotificationCategory"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:textColor="#0F766E"
                            android:textSize="11sp"
                            android:textStyle="bold"
                            android:fontFamily="sans-serif-medium"
                            android:letterSpacing="0.02"
                            android:paddingHorizontal="10dp"
                            android:paddingVertical="5dp"
                            tools:text="New Order" />
                    </androidx.cardview.widget.CardView>
                </LinearLayout>
            </LinearLayout>

            <!-- Arrow action button — teal tint -->
            <ImageButton
                android:id="@+id/btnNotificationAction"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="View details"
                android:padding="8dp"
                android:src="@drawable/ic_arrow_forward"
                app:tint="#14B8A6" />
        </LinearLayout>
    </LinearLayout>

</androidx.cardview.widget.CardView>