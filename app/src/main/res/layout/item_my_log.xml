<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Items -->
        <TextView
            android:id="@+id/tvLogItems"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Milk, Bread, Eggs"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#333333"
            android:maxLines="2"
            android:ellipsize="end"
            android:layout_marginBottom="8dp" />

        <!-- Pickup and Drop in a row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginBottom="8dp">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tvLogPickup"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="From: Market"
                    android:textSize="14sp"
                    android:textColor="#666666"
                    android:drawableStart="@android:drawable/ic_menu_mylocation"
                    android:drawablePadding="4dp"
                    android:drawableTint="#FF9800" />
            </LinearLayout>

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tvLogDrop"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="To: Hostel"
                    android:textSize="14sp"
                    android:textColor="#666666"
                    android:drawableStart="@android:drawable/ic_dialog_map"
                    android:drawablePadding="4dp"
                    android:drawableTint="#FF5722" />
            </LinearLayout>
        </LinearLayout>

        <!-- Status and Date Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="8dp">

            <TextView
                android:id="@+id/tvLogStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="OPEN"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="#FF9800"
                android:paddingHorizontal="12dp"
                android:paddingVertical="4dp"
                android:background="@drawable/bg_input_field"
                android:layout_marginEnd="12dp" />

            <TextView
                android:id="@+id/tvLogDate"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Oct 26, 2024 14:30"
                android:textSize="12sp"
                android:textColor="#999999" />
        </LinearLayout>

        <!-- Reward -->
        <TextView
            android:id="@+id/tvLogReward"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Reward: 10%"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="#4CAF50" />

    </LinearLayout>

</androidx.cardview.widget.CardView>